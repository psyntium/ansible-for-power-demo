---
- name: AIX Boostrap
  hosts: all
  gather_facts: no
  collections:
   - ibm.power_aix

  vars:
      pkgtype: "{{ pkgtype }}"

  tasks:

  - name: "AIX Boostrap {{ pkgtype }}"
    include_role:
      name: power_aix_bootstrap
    vars:
      pkgtype: "{{ pkgtype }}"