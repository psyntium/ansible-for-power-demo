---
- name: AIX Boostrap
  hosts: all
  gather_facts: no
  collections:
   - ibm.power_aix

  tasks:

  - name: AIX Boostrap
    include_role:
      name: power_aix_bootstrap
    vars:
      pkgtype: yum